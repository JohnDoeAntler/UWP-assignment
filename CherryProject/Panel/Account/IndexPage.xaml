<Page
    x:Class="CherryProject.Panel.Account.IndexPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CherryProject.Panel.Account"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <NavigationView x:Name="NavigationViewControl" PaneDisplayMode="Left"
            Background="{ThemeResource SystemControlAltLowAcrylicElementBrush}"
            PaneTitle="Account Management"
            IsBackEnabled="True"
            BackRequested="OnBackRequested"
            Margin="0 -1 0 0"
            SelectionChanged="OnSelectionChanged"
            ItemInvoked="OnItemInvoked">
            <NavigationView.AutoSuggestBox>
                <AutoSuggestBox PlaceholderText="Search" QueryIcon="Find"/>
            </NavigationView.AutoSuggestBox>
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Main Page" />
                <NavigationViewItemSeparator />
                <NavigationViewItem Tag="va" Icon="ContactInfo" Content="View Account" />
                <NavigationViewItem Tag="sa" Icon="Find" Content="Search Account" />
                <NavigationViewItem Tag="ca" Icon="AddFriend" Content="Create Account" />
                <NavigationViewItem Tag="ma" Icon="ContactPresence" Content="Modify Account" />
                <NavigationViewItem Tag="da" Icon="BlockContact" Content="Disable Account" />
            </NavigationView.MenuItems>
            <ScrollViewer>
                <StackPanel Padding="40">
                    <TextBlock x:Name="header" FontSize="32" FontWeight="Light" Text="" Margin="0 0 0 45"/>
                    <Frame x:Name="contentFrame"></Frame>
                </StackPanel>
            </ScrollViewer>
        </NavigationView>
        <Grid x:Name="titleBar" Background="Transparent" Margin="40 0 0 0" Height="40" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0"/>
            </Grid.ColumnDefinitions>
            <TextBlock/>
        </Grid>
    </Grid>
</Page>
